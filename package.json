{
  "name": "opencode-huge-agents",
  "version": "0.6.0",
  "description": "OpenCode agent pack: huge-plan (build-focused planning), ask (read-only), and exec (planning-first executor with approval menu)",
  "type": "module",
  "bin": {
    "opencode-huge-agents": "dist/cli.js"
  },
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "node --input-type=module -e \"import { rmSync } from 'node:fs'; rmSync('dist', { recursive: true, force: true });\" && tsc -p tsconfig.json",
    "test": "bun test",
    "postinstall": "node -e \"console.log('opencode-huge-agents installed with 3 agents: huge-plan, ask, exec\\nTo continue setup, run: npx opencode-huge-agents install\\nTo refresh later, run: npx opencode-huge-agents autoupdate\\nWith Bun: bunx opencode-huge-agents install')\"",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "opencode",
    "plugin",
    "agents",
    "planning",
    "ask",
    "exec",
    "orchestration",
    "approval-based"
  ],
  "license": "MIT",
  "author": "yurirxmos",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yurirxmos/opencode-huge-agents.git"
  },
  "bugs": {
    "url": "https://github.com/yurirxmos/opencode-huge-agents/issues"
  },
  "homepage": "https://github.com/yurirxmos/opencode-huge-agents#readme",
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.1.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.2.10",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1"
  }
}
